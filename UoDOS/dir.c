#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc, char *argv[])
{
	printf(argv[0]);
    printf("\n");
    printf("User program executed \n");

    //int result = opendir(directory);

	// if(result == 0)
	// {
	// 	return;
	// }

	// if(readdir() == -1)
	// {
	// 	return;
	// }

	// if(closedir() == -1)
	// {
	// 	return;
	// }
    exit();
}